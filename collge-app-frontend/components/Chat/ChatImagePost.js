import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ChatImagePost = ({ authorAvatar, authorUsername }) =>
{
    return (
        <View>
            <ChatImagePost authorAvatar={authorAvatar} authorUsername={authorUsername} />
        </View>
    )
}

export default ChatImagePost

const styles = StyleSheet.create({


})