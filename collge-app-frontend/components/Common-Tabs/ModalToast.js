import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ModalToast = ({ text }) =>
{
    return (
        <View>
            <Text>{text}</Text>
        </View>
    )
}

export default ModalToast

const styles = StyleSheet.create({})