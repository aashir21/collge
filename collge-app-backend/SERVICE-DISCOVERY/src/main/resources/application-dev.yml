
eureka:
  instance:
    hostname: "localhost"
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka


spring:
  application:
    name: "EUREKA-SERVER"

server:
  port: 8761